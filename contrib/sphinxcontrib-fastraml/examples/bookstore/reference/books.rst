Books
=====

The catalogue itself, and every endpoint below it (``:depth: -1``).

.. raml:endpoint:: /books
   :depth: -1

   Writes to the catalogue are limited to ten a minute per token; reads are
   not limited.
