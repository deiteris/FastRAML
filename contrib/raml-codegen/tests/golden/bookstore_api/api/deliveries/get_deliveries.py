# Generated by raml-codegen from a RAML 1.0 definition. Do not edit.
from __future__ import annotations

import datetime
from http import HTTPStatus
from typing import Any, Literal

import httpx

from ... import errors
from ...client import AuthenticatedClient
from ...models.delivery import Delivery
from ...types import UNSET, Mismatch, Response, Unset, as_list, reading

PATH = '/deliveries'
"""The path this module calls, before its `{...}` tokens are filled in."""

METHOD = 'get'
"""The HTTP method this module uses."""

SCHEMES = ('machineToken', 'oauth2')
"""The `securedBy:` schemes this operation accepts.

`security.py` holds the mechanics of each: which header carries the credential,
and what precedes it.
"""


def _kwargs(
    *,
    since: datetime.datetime,
    status: Literal['pending', 'sent', 'delivered'] | Unset = UNSET,
    city: str | Unset = UNSET,
    carrier: Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon', 'evri-standard'] | Unset = UNSET,
) -> dict[str, Any]:
    path = PATH

    params: dict[str, Any] = {}
    params['since'] = since.isoformat()
    if not isinstance(status, Unset):
        params['status'] = status
    if not isinstance(city, Unset):
        params['city'] = city
    if not isinstance(carrier, Unset):
        params['carrier'] = carrier

    headers: dict[str, str] = {}

    out: dict[str, Any] = {'method': METHOD, 'url': path}
    if params:
        out['params'] = params
    if headers:
        out['headers'] = headers
    return out


def _parse(*, client: AuthenticatedClient, response: httpx.Response) -> list[Delivery] | None:
    if response.status_code == 200:
        payload = response.json()
        return [Delivery.from_dict(_item) for _item in as_list(payload)]
    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    return None


def _build(*, client: AuthenticatedClient, response: httpx.Response) -> Response[list[Delivery]]:
    """The response, and whatever could be made of its body.

    Nothing a *payload* does gets out of here. A body that is not what the
    document describes -- a missing property, an unreadable date, HTML where
    JSON was promised -- lands in `mismatches`, and `content` still holds the
    bytes. Only `errors.UnexpectedStatus`, which the caller asked for, and a
    transport error, which is not about the payload, travel further.
    """
    parsed: list[Delivery] | None = None
    with reading(strict=client.strict) as mismatches:
        try:
            parsed = _parse(client=client, response=response)
        except errors.ClientError:
            raise
        except Exception as unreadable:  # noqa: BLE001 - a payload may be anything at all
            mismatches.append(Mismatch('list[Delivery]', '', f'{type(unreadable).__name__}: {unreadable}'))
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=parsed,
        mismatches=tuple(mismatches),
    )


def sync_detailed(
    *,
    client: AuthenticatedClient,
    since: datetime.datetime,
    status: Literal['pending', 'sent', 'delivered'] | Unset = UNSET,
    city: str | Unset = UNSET,
    carrier: Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon', 'evri-standard'] | Unset = UNSET,
) -> Response[list[Delivery]]:
    r"""List deliveries

    Every delivery, newest first.

    Args:
        since (datetime.datetime): Only deliveries promised on or after this instant.
        status (Literal['pending', 'sent', 'delivered'])
        city (str): Matched against the address, case-insensitively.
        carrier (Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon',
            'evri-standard']): Only deliveries handed to this carrier.

    Responses:
        200

    Raises:
        errors.UnexpectedStatus: the server answered with an undocumented status
            and the client was built with raise_on_unexpected_status.
        httpx.TimeoutException: the request took longer than client.timeout.

    Returns:
        Response[list[Delivery]] -- the status, the raw content and the parsed body.
    """

    response = client.get_httpx_client().request(**_kwargs(
        since=since,
        status=status,
        city=city,
        carrier=carrier,
    ))
    return _build(client=client, response=response)


def sync(
    *,
    client: AuthenticatedClient,
    since: datetime.datetime,
    status: Literal['pending', 'sent', 'delivered'] | Unset = UNSET,
    city: str | Unset = UNSET,
    carrier: Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon', 'evri-standard'] | Unset = UNSET,
) -> list[Delivery] | None:
    r"""List deliveries

    Every delivery, newest first.

    Args:
        since (datetime.datetime): Only deliveries promised on or after this instant.
        status (Literal['pending', 'sent', 'delivered'])
        city (str): Matched against the address, case-insensitively.
        carrier (Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon',
            'evri-standard']): Only deliveries handed to this carrier.

    Responses:
        200

    Raises:
        errors.UnexpectedStatus: the server answered with an undocumented status
            and the client was built with raise_on_unexpected_status.
        httpx.TimeoutException: the request took longer than client.timeout.

    Returns:
        list[Delivery] -- the parsed body of the 200 response, or None.
    """

    return sync_detailed(
        client=client,
        since=since,
        status=status,
        city=city,
        carrier=carrier,
    ).parsed


async def asyncio_detailed(
    *,
    client: AuthenticatedClient,
    since: datetime.datetime,
    status: Literal['pending', 'sent', 'delivered'] | Unset = UNSET,
    city: str | Unset = UNSET,
    carrier: Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon', 'evri-standard'] | Unset = UNSET,
) -> Response[list[Delivery]]:
    r"""List deliveries

    Every delivery, newest first.

    Args:
        since (datetime.datetime): Only deliveries promised on or after this instant.
        status (Literal['pending', 'sent', 'delivered'])
        city (str): Matched against the address, case-insensitively.
        carrier (Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon',
            'evri-standard']): Only deliveries handed to this carrier.

    Responses:
        200

    Raises:
        errors.UnexpectedStatus: the server answered with an undocumented status
            and the client was built with raise_on_unexpected_status.
        httpx.TimeoutException: the request took longer than client.timeout.

    Returns:
        Response[list[Delivery]] -- the status, the raw content and the parsed body.
    """

    response = await client.get_async_httpx_client().request(**_kwargs(
        since=since,
        status=status,
        city=city,
        carrier=carrier,
    ))
    return _build(client=client, response=response)


async def asyncio(
    *,
    client: AuthenticatedClient,
    since: datetime.datetime,
    status: Literal['pending', 'sent', 'delivered'] | Unset = UNSET,
    city: str | Unset = UNSET,
    carrier: Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon', 'evri-standard'] | Unset = UNSET,
) -> list[Delivery] | None:
    r"""List deliveries

    Every delivery, newest first.

    Args:
        since (datetime.datetime): Only deliveries promised on or after this instant.
        status (Literal['pending', 'sent', 'delivered'])
        city (str): Matched against the address, case-insensitively.
        carrier (Literal['royal-mail-tracked-48-signed-for', 'dpd-next-day-before-noon',
            'evri-standard']): Only deliveries handed to this carrier.

    Responses:
        200

    Raises:
        errors.UnexpectedStatus: the server answered with an undocumented status
            and the client was built with raise_on_unexpected_status.
        httpx.TimeoutException: the request took longer than client.timeout.

    Returns:
        list[Delivery] -- the parsed body of the 200 response, or None.
    """

    return (
        await asyncio_detailed(
        client=client,
        since=since,
        status=status,
        city=city,
        carrier=carrier,
        )
    ).parsed
