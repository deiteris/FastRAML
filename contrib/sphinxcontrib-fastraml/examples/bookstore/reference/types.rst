Types
=====

.. raml:types::

Shared types
------------

These are declared in the libraries the API uses. They are named by the file
that declares them, such as ``sample/common.raml#Address``, because the
``uses:`` key is the root file's own spelling for a library.

.. raml:types::
   :file: sample/common.raml

.. raml:types::
   :file: shared/measures.raml
